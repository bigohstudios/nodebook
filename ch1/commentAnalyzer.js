var Normalizer        = require("./commentNormalizer")
  , comment           = "Biebs 4evs!"
  , normalizedComment = Normalizer.normalize(comment)

console.log(normalizedComment)

// Code that does something with the comment
